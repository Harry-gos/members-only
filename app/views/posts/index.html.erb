<h1>Posts</h1>
<ul>
  <% !current_user.nil? && @posts = @posts.eager_load(:user)%>
  <% @posts.each do |post| %>
    <li>
      <p>Link: <%= link_to post.link, post.link %> </p>
      <% user_signed_in? && ( %>
      <p>Author: <%= post.user.email %></p>
			<% ) %>
    </li>
  <% end %>
</ul>

